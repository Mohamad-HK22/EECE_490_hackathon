# Backend
PORT=4000
NODE_ENV=development

# Frontend (Create React App)
# These are set automatically when running `npm start` in the frontend folder
# The proxy to the backend API is configured in frontend/package.json
REACT_APP_API_BASE=http://localhost:4000
