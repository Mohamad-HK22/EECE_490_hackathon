# Copy this to .env and fill in your OpenAI key
# cp .env.example .env

# Get a free key at https://platform.openai.com (uses gpt-4o-mini, very cheap)
# Leave blank to use built-in data-driven descriptions (app still works fully)
OPENAI_API_KEY=sk-...your-key-here...

PORT=4000
